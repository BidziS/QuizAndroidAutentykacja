<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bidzis.quizandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MenuActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".rozgrywka.WybierzTrybActivity" />
        <activity android:name=".rozgrywka.WybierzKategorieActivity" />
        <activity android:name=".sign_up_in.LogowanieActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sign_up_in.RejestracjaActivity" />
        <activity android:name=".rozgrywka.TrybNormalnyActivity" />
        <activity android:name=".ranking.RankingGlobalnyActivity" />
        <activity android:name=".ranking.RankingLokalnyActivity" />
        <activity android:name=".ranking.RankingWybierzActivity" />
        <activity android:name=".rozgrywka.TrybCzasowyActivity"></activity>
    </application>

</manifest>